name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: sxzz/workflows/.github/workflows/release.yml@v2
    with:
      publish: true
      build: pnpm build
    permissions:
      contents: write
      id-token: write
